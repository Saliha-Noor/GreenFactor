version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ASSUMED_CPU_TDP_WATTS=15

  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
